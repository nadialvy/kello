{
  "buildCommand": "if [ ! -d flutter ]; then git clone https://github.com/flutter/flutter.git -b stable --depth 1; fi && flutter/bin/flutter doctor && flutter/bin/flutter clean && flutter/bin/flutter pub get && flutter/bin/flutter build web --release",
  "outputDirectory": "build/web",
  "framework": null,
  "installCommand": "echo 'Skipping install'",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
